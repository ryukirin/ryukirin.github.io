---
title: "GitHub のコードを Colab で実行した記録（失敗）"
description: "Emotional Chatting Machine の GitHub コードを Colab で動かそうとした記録。Python 3.5、TensorFlow 1.0.1、CUDA 8.0 の環境構築まではできたが、ファイル不足で実行は失敗した。"
date: "2022-08-30T20:36:00+08:00"
tags: ["Colaboratory"]
---

# GitHub のコードを Colab で実行した記録（失敗）

## 前提

対象プロジェクトは論文 [Emotional Chatting Machine: Emotional Conversation Generation with Internal and External Memory](https://arxiv.org/abs/1704.01074) のコード。

プロジェクトアドレス: https://github.com/tuxchow/ecm

元のプロジェクトでは以下が必要だった。

- Python 2.7

- Numpy

- Tensorflow 0.12

Colab 上で扱うのはかなり大変だった（少なくとも自分には）ので、高めのバージョンに対応した代替として [ECM-tf](https://github.com/tuxchow/ECM-tf) を探した。

このプロジェクトで必要なものは以下。

- Python 3.5

- Tensorflow 1.0.1

- Numpy

最終的には、このプロジェクトはファイル不足（たぶん）で実行成功しなかった。ただし環境構築まではできたので、記録として残しておく。

## Colab の使用と環境構築

### Colab で GitHub のコードを取り込む

Google Drive をマウントする。

```
import os
os.chdir("/content/drive/My Drive")
```

GitHub のコードを取得する。

```
!git clone https://github.com/tuxchow/ECM-tf.git
```

プロジェクトフォルダに入る。

```
cd ECM-tf/
------------
/content/drive/My Drive/ECM-tf
```

ディレクトリを確認する。

```
!ls
-------------
apply.py      emo_embedding  test_medium.py   train.py
data_dict.py  internal_mem   test.py	      train_test_split.py
data.py       model	     train_medium.py
ECM	      README.md      train_mini.py
```

### Python バージョンを変更する

Colab の現在のデフォルト Python バージョンは 3.7 なので、Python 3.5 に変更する必要がある。

まず Python 3.5 をダウンロードする。

```
!apt-get install python3.5
```

あとで `pip` を使ったときに 3.7 環境へパッケージが入らないよう、Python 3.7 側の pip をアンインストールする。

```
!python -m pip uninstall pip
```

環境を 3.5 に切り替える。

```
!update-alternatives --install /usr/local/bin/python3 python3 /usr/bin/python3.5 2
!update-alternatives --install /usr/local/bin/python3 python3 /usr/bin/python3.7 1
!python --version
--------------------
update-alternatives: renaming python3 link from /usr/bin/python3 to /usr/local/bin/python3
update-alternatives: using /usr/bin/python3.5 to provide /usr/local/bin/python3 (python3) in auto mode
Python 3.5.10
```

Python 3.5 に pip をインストールする。

```
!apt-get install python3-pip
```

### Numpy をインストールする

アップグレードする（アップグレードしないと Numpy をインストールできなかった）。

```
!python3.5 -m pip install --upgrade pip
```

Numpy をインストールする。

```
!python3.5 -m pip install Numpy
```

### TensorFlow 1.0.1 をインストールする

プロジェクトの要求は TensorFlow 1.0.1。

そのため CUDA の互換性問題があり、デフォルトの CUDA をアンインストールして CUDA 8.0 を入れる必要がある。

```
!apt-get remove cuda
!apt-get autoremove cuda
!apt-get purge cuda
!apt-key del /var/cuda-repo-9-2-local/*.pub
!rm -rf /var/cuda-repo-8-0-local-ga2/
```

CUDA 8.0 をインストールする。

```
!sudo wget https://developer.nvidia.com/compute/cuda/8.0/Prod2/local_installers/cuda-repo-ubuntu1604-8-0-local-ga2_8.0.61-1_amd64-deb
!sudo dpkg -i cuda-repo-ubuntu1604-8-0-local-ga2_8.0.61-1_amd64-deb
!sudo apt-get update
!sudo apt-get -y install cuda-8-0
```

現在の CUDA バージョンを確認する。

```
!nvcc --version
--------------------
nvcc: NVIDIA (R) Cuda compiler driver
Copyright (c) 2005-2016 NVIDIA Corporation
Built on Tue_Jan_10_13:22:03_CST_2017
Cuda compilation tools, release 8.0, V8.0.61
```

TensorFlow 1.0.1 をインストールする。

```
!python3.5 -m pip install tensorflow==1.0.1
```

### プロジェクトを実行する

```
!python3.5 test.py -m ECM
```

実行エラー。

```
FileNotFoundError: [Errno 2] No such file or directory: 'data/data_test.pkl'
```

おそらくファイルが足りない。

## 参考記事

[Colab运行GitHub代码](https://www.cnblogs.com/HuangYJ/p/13362945.html)

[colab修改python版本](https://blog.csdn.net/qq_42533927/article/details/119217527)

[protobuf requires Python ‘＞=3.7‘ but the running Python is 3.6.5的解决方法](https://blog.csdn.net/LucyGill/article/details/125268665)

[Google Colab Using GPU with Tensorflow version 1.0.0](https://gist.github.com/onuryartasi/7b861ff3cff77bcf68846db3bec0b2a6)

_Codex による翻訳。_
